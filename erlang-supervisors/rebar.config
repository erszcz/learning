{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [sups]}
]}.

{relx, [{release, {sups, "0.1.0"},
         [sups]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.
