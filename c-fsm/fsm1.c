#include <stdio.h>

typedef struct {
    int type;
    int len;
    void* val;
} tlv;

typedef enum {
    msg

typedef enum {
    bark,
    wag_tail,
    sit
} state;

state fsm_state

int main(int argc, const char *argv[])
{
    
    return 0;
}
